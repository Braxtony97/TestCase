# TestCase
Test project

1. Добавлен интерфейс IPlayer, реализован класс Player
2. Убран синглтон Player → заменён DI с .AsSingle()
3. AsSingle обеспечивает "безопасный Singleton", создаваемый контейнером DI. Один общий объект
4. Player создаётся контейнером
5. Значения атрибутов игрока легко меняются из разных частей программы из-за легкости получения экземпляра класса Player (В классе MainTest)
6. Параметры элементов экипировки можно задавать в рантайме через DI (Класс GameInitializer, метод CreateItems()), там же происходит добавление экипировки игроку
